{
  "name": "libra-iskopi",
  "version": "1.0.0",
  "private": true,
  "description": "Production website for LIBRA, obrt za usluge u građevinarstvu",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/assets/app.js && node scripts/validate.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
